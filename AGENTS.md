# AGENTS.md

專案規則（程式、測試、文件怎麼寫）放在 `CLAUDE.md`，裡面是一張「什麼時候該讀哪份規則檔」的對照表。

改動任何專案檔案（程式碼、測試、文件或設定）前，先讀 `CLAUDE.md`，再依表讀 `.claude/rules/` 底下對應的檔案。
開始任何工作前先讀 `CLAUDE.local.md`（若存在）：溝通方式、commit 規則、個人覆蓋設定與待補清單都在那裡。
