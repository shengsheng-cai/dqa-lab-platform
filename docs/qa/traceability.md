# DQA Lab Platform — Minimal Traceability

English · [繁體中文](traceability.zh-TW.md)

This matrix connects critical behavior to risk and automated evidence. Defect
evidence is optional and links only selected case studies. It intentionally
covers the high-risk Demo baseline rather than every route, fix, or UI element.

| Requirement | Expected behavior | Risk | Automated evidence | Defect evidence | Status |
|---|---|---|---|---|---|
| **REQ-AUTH-01** | Guest cannot remain on admin-only pages or mutate protected business state; frontend routes and page mounts respect the role, and every admin-only API route enforces authorization | R-01 | `backend/tests/test_guest_authorization.py`; `backend/tests/test_blocked_period_audit.py::test_blocked_period_write_rejects_non_admin_without_audit`; `tests/e2e/specs/guest-readonly.spec.js` | — | Covered |
| **REQ-AUTH-02** | Credentials never travel in a URL: the device feed handshake presents a single-use ticket that expires in 30 seconds, so nothing an access log can record is worth replaying | R-09 | `backend/tests/test_ws_auth.py`; `tests/e2e/specs/ws-auth.spec.js` | [BUG-011](BUG-011-websocket-handshake-carried-a-long-lived-admin-token.md) | Covered up to the reverse proxy; whether the deployment forwards the handshake subprotocol is confirmed by opening the deployed Space |
| **REQ-AUD-01** | Device blocked-period changes record the authenticated actor; if the audit write fails, the business change rolls back | R-03 | `backend/tests/test_blocked_period_audit.py` | — | Covered |
| **REQ-MNT-01** | A maintenance-blocked device cannot be selected or started; the schedule remains confirmed and can retry after maintenance | R-02 | `backend/tests/test_schedule_start_consistency.py::test_start_skipped_when_device_in_maintenance`; `::test_maintenance_keeps_confirmed_then_resumes`; `tests/e2e/specs/maintenance-block.spec.js` | [BUG-002](BUG-002-maintenance-device-auto-started.md) | Covered |
| **REQ-STATE-01** | A successful start keeps device, execution, schedule, fixture, audit, and cache state consistent even when the caller is cancelled | R-03 | `backend/tests/test_schedule_start_consistency.py`; `backend/tests/test_device_state.py::test_start_repeated_cancellation_waits_for_commit_and_publishes_cache`; `backend/tests/test_linkage.py`; `tests/e2e/specs/schedule-flow.spec.js` | — | Covered |
| **REQ-STATE-02** | If the execution record cannot be created, the device returns to IDLE, the schedule stays confirmed, and fixtures stay reserved | R-03 | `backend/tests/test_device_state.py::test_start_execution_failure_leaves_db_and_cache_unchanged`; `backend/tests/test_schedule_start_consistency.py::test_start_schedule_keeps_confirmed_when_execution_insert_fails`; `::test_manual_start_sop_reverts_when_execution_insert_fails` | [BUG-003](BUG-003-execution-insert-failure-left-zombie-running-state.md) | Covered |
| **REQ-STATE-03** | Stopping a running test does not complete its schedule: the schedule stays 進行中 and its fixtures stay held, because an abort is not a completion and only a person can close the case | R-03 | `backend/tests/test_simulator_schedule.py::test_manual_stop_keeps_schedule_running_and_fixture_untouched`; `::test_ad_hoc_manual_stop_ignores_future_schedule` | — | Covered for the service seam the simulator calls; the notification wording itself is not asserted, since no test drives the simulator loop |
| **REQ-UI-01** | After confirmation, the schedule row reconciles with backend state without manual refresh | R-04 | `tests/e2e/specs/schedule-flow.spec.js` | [BUG-001](BUG-001-schedule-status-not-refreshed-after-confirm.md) | Covered |
| **REQ-UI-02** | After fixture, schedule, or maintenance writes, persisted data and visible state match what the operator submitted without waiting for the 30/60-second polling fallback | R-04 | `tests/e2e/specs/fixture-loan.spec.js`; `tests/e2e/specs/schedule-flow.spec.js`; `backend/tests/test_maintenance.py::test_update_maintenance_can_clear_next_date` | — | Covered |
| **REQ-UI-03** | Stopping a running test asks first, naming the device and the test, so one misclick cannot destroy hours of work; emergency stop and the cooldown that follows it stay single-click, because an accident is not the place for an extra step; and while a device is in emergency the screen offers only the cooldown, never the flow for starting the next test | R-12 | `tests/e2e/specs/device-stop-confirm.spec.js`; `tests/e2e/specs/emergency-screen.spec.js` | — | Covered for the dialog, for cancelling, and for what the emergency screen offers |
| **REQ-UI-04** | Deleting a device unavailability window asks first and names the device, the window, and the reason, because that row is also what keeps scheduling and manual start off the machine | R-12 | `tests/e2e/specs/maintenance-block.spec.js` | — | Covered for the dialog and for cancelling |
| **REQ-UI-05** | Confirming a purchase arrival asks first and names the fixture, the arriving quantity, and the stock level before and after, because that one click also writes stock | R-12 | `tests/e2e/specs/purchase-arrival-confirm.spec.js` | — | Covered for the dialog and for cancelling |
| **REQ-UI-06** | The monthly stocktake never drops a fixture silently: items that cannot be counted because something is out on loan or reserved are listed as excluded with the reason, and counted plus excluded equals the catalogue; when nothing is countable the dialog says why instead of reporting a completed count | R-04 | `tests/e2e/specs/stocktake-scope.spec.js` | [BUG-014](BUG-014-monthly-stocktake-silently-omitted-fixtures.md) | Covered for the exclusion list and the total; the all-excluded case is held by a code guard, not by a test |
| **REQ-UI-07** | A confirmed schedule offers "start now" only when the chamber can actually take it: while the chamber is busy or sits inside a maintenance window the button is disabled and says which chamber, why, and when it frees up, instead of letting the operator spend a click on a request the backend will refuse | R-04 | `tests/e2e/specs/schedule-start-readiness.spec.js` | — | Covers the enabled fallback before WebSocket readiness, a maintenance block created in another browser, and a disabled continuation control while the chamber is cooling down |
| **REQ-UI-08** | A fixture's keeper has one editable source — the person picked in the keeper dialog. The edit-fixture form shows it read-only and the API refuses a keeper sent there, so an edit can never report success while leaving the screen unchanged; clearing a keeper asks first and names who is being cleared; and a keeper that is only a name with nobody behind it is labelled unlinked instead of passing for a keeper that was set on purpose | R-04 | `tests/e2e/specs/fixture-keeper.spec.js`; `backend/tests/test_fixtures_api.py::test_create_fixture_rejects_keeper_name`; `::test_update_fixture_rejects_keeper_name`; `::test_update_fixture_leaves_keeper_untouched`; `backend/tests/test_fixture_excel.py::test_import_links_keeper_to_user_when_name_matches`; `::test_export_uses_the_keepers_current_name`; `::test_import_leaves_columns_the_sheet_does_not_have_alone` | — | Covered |
| **REQ-UI-09** | Every write on the people-management page reports what happened: revoking a guest token names the key in the confirmation and again on success, and a failed revocation leaves both the dialog and the row in place, because a token that may still open the system must not vanish behind a screen that looks settled; copying a freshly issued token says whether it reached the clipboard, since the panel showing it never comes back; enabling or disabling a person names who it was; and deleting a person names who was removed on success while a failed deletion likewise leaves both the dialog and the row in place | R-12 | `tests/e2e/specs/users-page-feedback.spec.js` | — | Covered for the confirmation text, cancelling, the failed revocation, the copy fallback when the clipboard API is unavailable, both toggle outcomes, and both delete outcomes |
| **REQ-UI-10** | Deleting a purchase order, a schedule, or a maintenance record asks first, and the dialog names the record it is about to delete — the dialog covers the very row the operator would check against, so a bare "delete this?" leaves a misclick unrecoverable at the last step | R-12 | `tests/e2e/specs/delete-confirm-identity.spec.js` | — | Covered for the identifying text in all three dialogs and for cancelling; the maintenance one also asserts the browser's native dialog is gone |
| **REQ-UI-11** | Every main entry point works from the keyboard, not only the mouse: device cards, schedule rows, Gantt blocks, the standard and version pickers, sortable headers, the fixture import file picker, and stocktake batches are real buttons whose names say what they open; the current selection, expanded state, and sort direction reach assistive technology instead of being carried by colour alone; the focus ring is visible against the dark theme; and a dialog can be closed with Esc, takes focus when it opens, and hands focus back to whatever opened it when it closes — including when one dialog replaces another, and without stealing focus from a field that asks for it | R-13 | `tests/e2e/specs/keyboard-navigation.spec.js`; `client/eslint.config.js` | — | Covered for the seven entry points the spec drives, one of which walks the real Tab order rather than setting focus directly; dialog focus and Esc are covered for a plain open/close, a dialog replaced by another, a dialog stacked on a dialog, and an autofocused field. What a screen reader actually announces is not asserted, and there is no focus trap: Tab still reaches the page behind the dialog |
| **REQ-UI-12** | A list that fails to load says so instead of claiming there is nothing there: loading, a failed load, and a genuinely empty list are three different messages, and the failure names its cause — unreachable backend, missing permission, or the status code. A refresh that fails keeps the rows already on screen behind a banner saying they are stale, and downloads report why they failed instead of leaving the button to spring back in silence. The guest token list in particular can never ask an admin to issue a fresh credential just because the fetch failed | R-04 | `tests/e2e/specs/list-load-errors.spec.js`; `client/src/__tests__/loadError.test.js` | — | Covered for the four record lists and the sidebar counters, with the failure injected at the network layer; other lists (error log, schedule, standards picker) still report a failed fetch as emptiness |
| **REQ-UI-13** | A long form keeps its main actions reachable: a dialog is split into a fixed title, a scrolling body, and a fixed action bar, so the submit button never scrolls out of sight on a laptop-height window. The message a failed submit produces sits with the buttons rather than in the scrolling area, because a button that stays put while its error scrolls away reads as a click that did nothing | R-04 | `tests/e2e/specs/modal-layout.spec.js` | — | Covered at a 1280×620 viewport for the schedule and fixture dialogs and for a failed submit injected at the network layer; the dialogs not driven by the spec inherit the same three-part frame |
| **REQ-UI-14** | A row action cannot turn a mis-click into a delete: every button is at least 32px tall, delete comes last and is spaced away from the button before it, and the group never wraps — wrapping puts delete at the start of a second line, directly beneath the first button. Delete carries a neutral border with red text only, so it is not the loudest button in a row of small ones | R-12 | `tests/e2e/specs/row-action-hit-area.spec.js` | — | Covered for the fixture table (including the four-button row that appears when a fixture is out of stock), both maintenance tables, and user management, measured as rendered geometry rather than compared as style strings; the other rows sharing the same styles (purchases, loan details, stocktake batches, execution records) have no assertions of their own |
| **REQ-UI-15** | A form says which required field is missing instead of swallowing the click: the label carries an asterisk before anything is typed, and submitting with a field empty writes the missing field's name where the operator is already looking rather than returning in silence. The login page has no labels to mark, so it moves focus to the field it is asking for and puts the message in the card's alert region — the only one available before the toast provider mounts | R-04 | `tests/e2e/specs/login-required-fields.spec.js`; `tests/e2e/specs/users-page-feedback.spec.js` | — | Covered for the account and password fields, the guest token, the Enter key, and the required markers on the people form; the login spec also asserts the guard answers without reaching the backend. The other forms name the missing field but do not move focus |
| **REQ-SCH-01** | Active schedules do not overlap on one device; confirmed time edits replace the exact-start job; temporary blocks retry; invalid schedules converge to a terminal error | R-05 | `backend/tests/test_schedule_conflict.py`; `test_schedules_slot.py`; `test_schedule_start_consistency.py::test_confirmed_slot_edit_replaces_scheduled_start_job`; `test_simulator_schedule.py` | [BUG-002](BUG-002-maintenance-device-auto-started.md) | Covered |
| **REQ-SCH-02** | A device returning to ambient counts as occupied until it reaches ambient; the device card and the scheduler derive that moment from one shared estimate | R-05 | `backend/tests/test_utils.py`; `backend/tests/test_schedules_complete.py::test_est_end_finishing_counts_remaining_ramp_not_whole_curve`; `::test_est_end_finishing_after_emergency_is_not_treated_as_free`; `::test_build_running_until_includes_finishing_device` | [BUG-006](BUG-006-cooling-device-treated-as-available.md) | Covered |
| **REQ-TIME-01** | Every timestamp a client submits is converted to UTC before it is stored, whichever endpoint receives it, so a client sending a non-UTC offset cannot silently shift a schedule window, a due date, or a report window | R-05 | `backend/tests/test_datetime_normalization.py`; `backend/tests/test_reports_degradation.py::test_non_utc_timestamps_are_converted_before_saving` | — | Covered across schedule slots, blocked periods, fixture loan and extension, calibration and maintenance, and execution records |
| **REQ-FIX-01** | Fixture quantity remains nonnegative across API and Excel writes; reserve → loan → return and purchase-arrival transitions never inflate stock or affect another schedule; a schedule confirmation cannot reserve more than the remaining stock; two allocations overlapping in time cannot both claim the same remaining unit | R-06 | `backend/tests/test_fixture_lifecycle.py` (incl. `::test_manual_loan_and_schedule_cannot_both_claim_last_fixture`); `test_fixtures_api.py`; `test_fixture_excel.py`; `test_purchase_orders.py`; `test_linkage.py`; `tests/e2e/specs/fixture-loan.spec.js` | [BUG-003](BUG-003-execution-insert-failure-left-zombie-running-state.md); [BUG-010](BUG-010-concurrent-loan-and-schedule-both-claimed-the-last-fixture.md) | Covered; the concurrent case is pinned on the in-memory test database, and the file-backed path the deployment uses was verified separately at fix time |
| **REQ-FIX-02** | Dates the operator submits are recorded against the operator's local day, not the UTC one: the return date is the local calendar day, a due date expires at the end of its local day, and 今日到期 counts the local day | R-06 | `client/src/__tests__/timezone.test.js` (suite pinned to `Asia/Taipei`); `backend/tests/test_fixtures_api.py::test_summary_due_today_uses_caller_day_window`; `::test_summary_without_window_falls_back_to_utc_day` | [BUG-004](BUG-004-fixture-dates-stored-one-day-early.md); [BUG-005](BUG-005-fixture-day-deadlines-evaluated-in-utc.md) | Covered |
| **REQ-FIX-03** | Returning a fixture goes through the return dialog, so the operator can record a condition, a note, and the actual return date, with a second confirmation for damaged or lost | R-12 | `tests/e2e/specs/fixture-loan.spec.js` | — | Covered |
| **REQ-EXT-01** | LINE, report, and AI provider failures are contained and return actionable results | R-07 | `backend/tests/test_line_resilience.py`; `test_reports_degradation.py`; `test_ai_observability.py` | — | Covered with mocked failures |
| **REQ-RPT-01** | A saved execution carries the timestamps its report needs, so the measurement summary reflects the test window whichever path saved the record | R-07 | `backend/tests/test_reports_degradation.py::test_frontend_iso_timestamps_still_match_sensor_data`; `client/src/__tests__/executionPayload.test.js` | [BUG-007](BUG-007-fallback-execution-save-diverged-from-main-path.md) | Backend contract covered; both frontend save paths now build the request body through one shared function, so omitting a field fails a test (remaining gap: GAP-04) |
| **REQ-RPT-02** | A report's measurement summary (max/min/average) agrees with its own uncertainty analysis, and the same execution's PDF and CSV reports agree with each other | R-07 | `backend/tests/test_reports_degradation.py::test_summary_stats_matches_uncertainty_mean_not_full_window_average`; `::test_csv_report_avg_temp_matches_uncertainty_stable_segment`; `backend/tests/test_uncertainty.py::test_stable_segment_filter` | [BUG-008](BUG-008-report-summary-disagreed-with-uncertainty-analysis.md) | Covered |
| **REQ-RPT-03** | A report identifies the item that was tested, not the chamber that ran it; when an execution has no case behind it the report says so rather than substituting equipment identification | R-07 | `backend/tests/test_reports_degradation.py::test_report_identifies_the_sample_not_the_chamber`; `::test_report_states_no_case_when_execution_has_no_schedule`; `::test_saved_execution_inherits_the_case_from_the_row_created_at_test_start`; `::test_saved_execution_does_not_borrow_the_case_of_a_later_schedule`; `::test_start_row_timestamp_matches_the_one_the_browser_gets_back`; `::test_device_api_does_not_truncate_the_timestamp_the_browser_sends_back` | [BUG-009](BUG-009-report-test-item-section-identified-the-chamber-not-the-sample.md) | Covered for CSV against rendered text; PDF shares the same case-resolution helper but its rendered content is not asserted (no PDF parser in this baseline) |
| **REQ-AI-01** | AI-recommended conditions can enter scheduling, while guest remains unable to submit a write | R-08 | `backend/tests/test_rag.py`; `tests/e2e/specs/ai-apply-schedule.spec.js`; `guest-readonly.spec.js` | — | Covered with mocked AI boundary |
| **REQ-AI-02** | When AI is not configured the panel says so in place and disables the input and the example questions, instead of letting someone type into a form that cannot send; a failed send shows the reason the backend gave rather than a generic connection error; an expired token returns to the login page | R-04 | `backend/tests/test_runtime_info.py`; `tests/e2e/specs/ai-disabled.spec.js`; `tests/e2e/specs/list-load-errors.spec.js`; `client/src/__tests__/loadError.test.js` | — | Covered; verified for both admin and guest |
| **REQ-OPS-01** | When core background work (simulator, scheduler) stops, the health probe fails and names which one; a background task's exception is always retrieved and logged rather than left to garbage-collection time | R-10 | `backend/tests/test_health.py` | [BUG-012](BUG-012-dead-simulator-task-still-reported-a-healthy-service.md) | Covered as far as whether the probe tells the truth; the deployment has no consumer today, so whether anyone is told after a 503 is outside the suite |
| **REQ-DATA-01** | Declared relationships are enforced by the database: deleting a row never leaves another row pointing at it, and each foreign key states whether the reference is emptied, carried away, or refused | R-11 | `backend/tests/test_foreign_key_enforcement.py`; `backend/tests/test_schema_migrations.py` | [BUG-013](BUG-013-declared-foreign-keys-were-never-enforced.md) | Covered for the schema as declared; repairing a database that already contains orphans is refused by the migration rather than automated |

## Portfolio scope exclusions

| Excluded item | Reason |
|---|---|
| Real chamber integration | The public portfolio baseline is simulator-only. It demonstrates workflow and QA automation, but does not claim vendor-protocol or physical-control validation. Any authorized real-device work belongs on a separate branch and is not an open gap in this baseline. |
| Load, multi-browser, and accessibility matrices | The public portfolio baseline targets Playwright-managed Chromium and selected high-risk flows. It does not claim production-scale capacity, cross-browser certification, or accessibility conformance; create separate plans only if deployment requirements change. |

## Open coverage gaps

| Gap | Impact | Planned treatment |
|---|---|---|
| **GAP-04** — Frontend fallback paths that fire only on a dropped device-feed update | When the fallback fires, and how it merges the result back into state, is still verified by code review rather than by a test | Narrowed: the request body both save paths send is now built by one shared function (`client/src/utils/executionPayload.js`) and pinned field by field by `client/src/__tests__/executionPayload.test.js`, so the field drift behind BUG-007 now fails a test. The trigger condition and state merge still need component tests, which this baseline excludes |
